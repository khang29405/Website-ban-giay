server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;
    absolute_redirect off;

    location = / {
        return 302 /html/index.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /html/404.html;
}
